class Ops():

    matrix=None
    self.is_square=None

    def __init__(self, matrix):
        self.matrix=matrix[:-1]
        self.dim=matrix[-1]
        self.is_square=self.dimCheck()

        if is_square:

        else:
            print('Error: Input matrix must be of nxn (square) dimensions.')

    def dimCheck(self):
        if self.dim[0]==self.dim[1]:
            return True
        else
            return False

    
    def Det(self):
        
        d=0

        if self.dim[0]==2:

            for i in range(0, 2):
                
